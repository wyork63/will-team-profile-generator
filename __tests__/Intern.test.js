const { TestWatcher } = require('jest');
const Intern = require('../lib/Intern');

// creates intern object
TestWatcher('creates an Intern object', () => {
    const intern = new Intern ('Will', 124, )
})